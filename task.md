# Preview and Edit Flows

- `[ ]` Update `MyProducts.jsx`
  - `[ ]` Add `selectedPreviewProduct` state
  - `[ ]` Import and render `ProductQuickViewModal`
  - `[ ]` Change Preview Listing button to open the modal
- `[ ]` Update `ProductDetails.jsx`
  - `[ ]` Import `AuthContext`
  - `[ ]` Detect if logged-in user is the owner
  - `[ ]` Conditionally render "Preview Mode / Edit" widget instead of "Request to Rent"
- `[ ]` Update `AddProduct.jsx`
  - `[ ]` Fetch product data if `id` is present in URL
  - `[ ]` Pre-fill form fields
  - `[ ]` Update submission logic to use `PUT` instead of `POST` when editing
